apiVersion: v1
kind: Service
metadata:
  name: clearing-prototype-client
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: clearing-prototype-client
  ports:
  - name: app
    protocol: TCP
    port: 6000
    targetPort: 4200